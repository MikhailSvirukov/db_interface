[package]
name = "client"
version = "0.1.0"
edition = "2021"

[dependencies]
core_app = { path = "../core" }
egui = "0.30.0"
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1.0", features = ["derive"] }
eframe = "0.30.0"
egui-modal = "0.6.0"
